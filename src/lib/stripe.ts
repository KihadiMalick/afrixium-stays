// Stripe client — à configurer quand STRIPE_SECRET_KEY sera disponible
// import Stripe from "stripe";
// export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, { apiVersion: "2024-09-30.acacia" });

export {};
